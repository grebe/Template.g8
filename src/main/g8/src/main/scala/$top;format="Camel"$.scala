package $top;format="Camel"$

import Chisel.{Complex => _, Mux => _, Reg => _, RegNext => _, RegInit => _, Pipe => _, Mem => _,
                 Module => _, ModuleOverride => _, when => _, switch => _, is => _, unless => _, Round => _,  _}
import ChiselDSP._

case class $top;format="Camel"$Params (
  // TODO: fill in parameters as needed
)

class $top;format="Camel"$[T <: DSPQnm[T]](gen : => T, p:$top;format="Camel"$Params)  extends GenDSPModule (gen) {
  // TODO: write your module
}
